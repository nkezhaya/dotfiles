To install GNU stow:

`$ brew install stow`

After installing, clone `dotfiles` to the home directory (`~`). Then install
tmux tpm (if stowing tmux):

`$ git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm`
